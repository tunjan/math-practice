/**
 * Any other student page. A parallel slot keeps showing what it last matched
 * on a client-side navigation, so this is what closes a task when the student
 * goes elsewhere.
 */
export default function NoTask() {
  return null
}
